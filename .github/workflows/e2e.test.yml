name: E2E Test
on:
  push
  schedule:
    cron: '30 5,17 * * *'
jobs:
  e2e-test:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
      - uses: actions/checkout@v3
      - name: Cypress Run
      - uses: cypress-io/github-action@v4
        with:
          browser: chrome